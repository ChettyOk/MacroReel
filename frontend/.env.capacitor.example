# Copy to .env.capacitor or set this in your shell before building the native app.
# Use your deployed Render backend URL, not localhost.
VITE_API_URL=https://your-macroreel-service.onrender.com
# Public web URL used when sharing recipes from the native app (optional).
# VITE_WEB_URL=https://your-macroreel-service.onrender.com
